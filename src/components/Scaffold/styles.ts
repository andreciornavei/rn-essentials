import { StyleSheet } from "react-native"

export default StyleSheet.create({
  container: { flex: 1 },
  body: { flex: 1 },
  bottom: {},
  floatContainer: {
    alignItems: "center",
    justifyContent: "center",
    position: 'absolute',
  }  
});
